#文件上传路径前缀，结尾必须加/
file-upload:
  file-path-prefix: /app/yazhe-web/file-repository/
spring:
  mvc:
    # 静态文件请求匹配方式
    static-path-pattern: /store/**
  resources:
    # 修改默认的静态寻址资源目录 多个使用逗号分隔
    static-locations: file:/app/yazhe-web/file-repository/